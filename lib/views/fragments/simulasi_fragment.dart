part of 'fragment.dart';

class SimulasiFragment extends StatelessWidget {
  const SimulasiFragment({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Column(
        children: [
          Text('test simulasi fragment'),
        ],
      ),
    );
  }
}