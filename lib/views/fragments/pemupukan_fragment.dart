part of 'fragment.dart';

class PemupukanFragment extends StatelessWidget {
  const PemupukanFragment({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Column(
        children: [
          Text('test pemupukan fragment'),
        ],
      ),
    );
  }
}