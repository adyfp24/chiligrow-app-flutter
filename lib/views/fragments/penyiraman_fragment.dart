part of 'fragment.dart';

class PenyiramanFragment extends StatelessWidget {
  const PenyiramanFragment({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Column(
        children: [
          Text('test penyiraman fragment'),
        ],
      ),
    );
  }
}